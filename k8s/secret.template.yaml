# Copy to secret.yaml and supply real values outside version control.
# CI must apply the populated Secret, never this empty template.
apiVersion: v1
kind: Secret
metadata:
  name: users-api-secret
  namespace: tds-group-3
type: Opaque
stringData:
  DATABASE_URL: ""
  REDIS_URL: ""
  JWT_PRIVATE_KEY: ""
