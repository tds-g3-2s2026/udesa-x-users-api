# CLAUDE.md

Las reglas de este repositorio están en `AGENTS.md`. Leelo antes de trabajar acá.

Este archivo es un puntero para agentes que buscan `CLAUDE.md` por convención; no dupliques
contenido acá. Se reescribe con `scripts/sync-comunes.sh`, no editar la copia local.
